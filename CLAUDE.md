# Claude Code Instructions

## Universal Agent Skills Integration
This repository uses the standardized `.agents` skill architecture.

### Primary Instructions
- Always reference `AGENTS.md` at the root of the workspace.
- The master skill orchestrator is `.agents/skills/using-agent-skills/SKILL.md`.
- Operating philosophy: `global-reasoning-layer` and `coderabbit-dna` are permanent cognitive foundations that govern every session.
- UI Design Workflow: You MUST execute `.agents/skills/anti-ai-design/SKILL.md` before writing any frontend code or component libraries.
- Complete execution sequences, skill triggers, and lifecycle stages are defined in `AGENTS.md` and `.agents/rules/skill-orchestration.md`.
